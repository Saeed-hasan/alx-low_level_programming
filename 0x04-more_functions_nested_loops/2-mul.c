#include"main.h"
/**
 * mul -  multiple two integer
 * @a : first input
 * @b : sec input
 * Return: return result
 */
int mul(int a, int b)
{
	int multiplication = a * b;
		return (multiplication);
}
